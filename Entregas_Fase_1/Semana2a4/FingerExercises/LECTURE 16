def flatten(L):

    result = []
    for elem in L:
        if isinstance(elem, list):
            result.extend(flatten(elem))
        else:
            result.append(elem)
    return result
 
L = [[1, 4, [6], 2], [[[3]], 2], 4, 5]
print(flatten(L))