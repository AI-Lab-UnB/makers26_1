# RESUMO TÉCNICO FUNDAMENTOS DE GIT E GITHUB
### Entrega 01 - Semana 01
#### Samuel De Souza Leite 202043281@aluno.unb.br

O Trabalho a seguir tem o objetivo de descrever os conceitos e formas de trabalho com o GIT e GITHUB e os comandos necessários para o fluxo de trabalho.

### 1. Conceitos básicos e diferenças entre GIT e GITHUB
- O GIT é um sistema de controle de versões descentralizado e distribuído e permite que cada desenvolvedor possua uma cópia completa do repositório localmente, funcionando como uma "máquina do tempo" do código. A maior vantagem é ser OpenSource e gratuito.
- O GITHUB é uma plataforma web para hospedar os repositórios e para organizar os trabalhos colaborativos. O Git oferece funcionalidades como gestão de tarefas, rastreamento de bugs, solicitações de funcionalidades e documentação.
- Esistem outros sistemas de controle de versão alé do git porém são menos utilziados como o SVN e MERCURIAL.


### 2. Principais características do GIT:
- É Descentralizado e distribuído, permite uma cópia local completa do repositório.
- Possui histórico de versões que é um registro de todas as alterações realizadas ao longo do tempo.
- Colaborativo, permite que múltiplos desenvolvedores tabalhem simultaneamente no mesmo projeto sem interferir no trabalho de outro desenvolvedor.
- Cria e genrecia Branches (ramificações), são linhas de desenvolvimento intependentes para funcionalidades ou correções.
- Permite o MERGE que é combinação de códigos de diferentes ramificações.
- Área de preparação (staging area), que é um local intermediaário onde as alterações são organizadas antes de serem registradas no histórico por meio de commit.

GIT é o sistema de controle de versaõ e GITHUB é a plataforma que contem os repositorios GIT

2 opções: Usar o terminal
ou uma interface gráfica
a recomendação é usar pelo terminal ao invés da interface (GUI)


CONFIguraNDO o GIT:

git congfi --global user.name "Seu nome"
git config --global user.email "seuemail@exemplo.com"


GITWORKFLOW:

Dentro do local machine:
- Working directory (onde faço as alterações dos arquivos)
- staging area (onde preparo os arquivos para o commit)
- Local repo (onde o git armazena as alterações feitas nos arquivos) salvos no .git arquivo
- Remote repository (github ou gitlab) onde armazena os repositorios e outras pessoas podem baixar.

Como usar?
GIT WORKGFLOW
inicializa com gitinit para criar o arquivo .git
depois git add para adicionar os arquivos para a staging area
depois git commmit com um comentario para explicar do que se trata o comit

git push para enviar para o repositorio remoto

o git pull serve para pegar os arquivo dentro do repositorio remoto para o working repository

git clone puxa do remote repo para o local repo

BRANCH & MERGING
permite para trabalhar em bugfiz sem afetar o código fonte principal.
criar diferentes branches para diferentes ambientes como production, staging, development.
Permite adicionar branches para diferentes features.

o Pull request é para mesclar seu código de uma branche na main branch.

----
passo a passo pratico

1- instalar o git 
2- configurar o nome e email com:
git config --global user.name "exemplo" e --user.email "exemplo"

3- configurar a branch padrão de Master para a  Main:
git condig -- global init.defaultBranch

4- para começar um novo projeto no git.
abre o vscode com:
code .
iniciar o git com:
git init
// para apagar um arquivo, escreva rm -rf "nome do arquivo"
// arquivos com a letra "u" ao lado do nome no vscode significa "untracked" e com a cor verde no nome, significa que não foram adicionados ao staging area. Ou seja, qualquer alteração não está sendo trackeado
O "source control" (simbolo de branches no vscode) serve para fazer o mesmo que é feito no terminal (mostra os arquivis untracked e afins)

escreva git add . no terminal para enviar todos os arquivos para a staging area, ou escreva git add "nome_do_arquivo.html" para enviar um arquivo em específico.

se o arquivo estiver com a letra "A" ao lado do noem do vscode, significa que ele foi adicionado ao staging area. 
e pode ser verificado com o git status.

git commit -m 'initial commit' serve para enviar arquivos da staging area para o local repo com uma mensagem de comentário para explicar do que se trata o commit

se o arquivo estiver com a cor laranja, significa que houve uma modificação no repositorio local e ele tambem cai ter a letra "M" ao lado do nome

os git commit -m "" serve para atualizar o repositorio local

git log serve para mostra os meus commits
começando pelo mais recentes primeiro e aperta "q" para sair do log

git push envia para o repostorio remoto.

se não tiver o repositorio para enviar, primieiro acessa o site do gitgub e cria um novo.

para dar push num repositorio já criado:
git remote add origin "enteredço do repositorio"

para usar a Main branch usa git branch -M main
depois para enviar o codigo:
git push -u origin main

geralmente criamos um readme localmente depois faz o push pro repositorio.

para pegar algo do repositorio:
git pull


---
o arquivo .gitignore serve para avisar ao git para não enviar um arquivo em especifico por exemplo um arquivi .env com chaves de API

para isso cria um arquivo .gitignore e dentro dele escreve o que tu não quer enviar, exeplo o .env

// se quiser adicioanr e já fazer o commit com uma unica linha de comando coloca a flag -a

exemplo:
git commit -am "Added task"

git status não vai mostrar nada, porque já pulou uma etapa

depois dá git push e estaraá no repo remoto

git add index.html && commit -m "Added task" . Dessa forma faz dois comandos ao mesmo tempo
depis dá git push para enviar para o remoto

## getting coding from gitbub.

git clone e copia a o link do github

o fetch serve para pegar o codigo do github sem dar merge

forking faz uma copia do repositorio para a propria conta

## para gerar uma nova chave ssh
ssh-keygen -t rsa -b 4096
 
 
